import { CalendarEvent } from '../types/event';

export class GoogleCalendarService {
  public static async syncWithGoogle(): Promise<CalendarEvent[]> {
    // Integration boilerplate for Google OAuth2 / Calendar API
    return [];
  }
}